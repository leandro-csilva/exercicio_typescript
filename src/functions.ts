function multiplicar(numero1: number = 5, numero2: number = 10): number {
    return numero1 * numero2;
}

console.log(multiplicar());

function concatenar(nome: string = "Leandro"): string {
    return "Olá " + nome;
}

console.log(concatenar());